[project]
name = "geohash"
version = "0.1.0"
description = "USGS earthquake catalog LSTM magnitude prediction using geohashes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "torch>=2.0.0",
]
